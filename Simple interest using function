#include <stdio.h>
int main() {
    int minutes, hours, rem;
    printf("Enter minutes: ");
    scanf("%d", &minutes);

    hours = minutes / 60;
    rem = minutes % 60;

    printf("%d hours %d minutes", hours, rem);
    return 0;
}
